# True Fantasy, App

Statischer Export der App. Kein Build, kein Server: `index.html` oeffnen oder das Verzeichnis bei Vercel als statisches Projekt anlegen.

Ohne Landingpage und ohne Anmeldung. Log out laedt neu und landet im Feed.

Gebaut mit `tools/build_app_export.py` aus dem Design-System.
